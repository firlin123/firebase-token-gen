{
  "name": "firebase-token-gen",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev:": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "non-prod-deploy": "wrangler versions upload"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "wrangler": "^4.21.2"
  }
}
